// The troop's home base — default origin for travel routes.
export const HOME_ADDRESS = "651 El Camino Real, Redwood City, CA";

// The app is mounted under this path on troop10rwc.org (same-origin tab).
// Everything — assets, API, auth, cookies, OAuth redirect — lives beneath it.
export const BASE_PATH = "/manage/expenses";
